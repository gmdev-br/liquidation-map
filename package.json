{
  "name": "liquidation-map",
  "version": "1.0.0",
  "description": "Liquidation Map Application",
  "homepage": ".",
  "scripts": {
    "start": "npx http-server -p 8000 -o",
    "predeploy": "npm install gh-pages --save-dev",
    "deploy": "gh-pages -d ."
  },
  "devDependencies": {
    "eslint": "^9.39.2",
    "eslint-formatter-compact": "^9.0.1"
  }
}